<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.road.of.android">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".global.DevApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".moudle.user.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.example.DialogExampleActivity"
            android:label="@string/title_activity_dialog_example"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.example.ToastExampleActivityActivity"
            android:label="@string/title_activity_toast_example_activity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".moudle.example.LogExampleActivity"
            android:label="@string/title_activity_log_example"
            android:theme="@style/AppTheme.NoActionBar"></activity>
    </application>

</manifest>